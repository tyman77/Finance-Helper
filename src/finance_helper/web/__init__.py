"""Local review UI: upload a vendor CSV, edit the proposed coding, approve/post.

Wraps the exact same pipeline the CLI uses (finance_helper.pipeline, categorize,
destinations, validate) — no logic is duplicated here.
"""

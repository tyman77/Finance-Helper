# Only needed to run the fetch scripts (scripts/fetch_*_index.py) against live
# Google Sheets / Calendar with a service account. Uses the requests transport
# (proxy-friendly) rather than httplib2.
google-auth>=2.23
requests>=2.31
